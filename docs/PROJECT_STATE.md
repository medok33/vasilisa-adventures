# Текущее состояние проекта

Дата фиксации: 8 сентября 2026 года.

## Подготовлен UI-005 — мобильная навигация (не production)

- Ветка: `ui-005-mobile-navigation`, база `b260fe45`.
- Возврат сохраняет прежнюю прокрутку, переходы используют историю браузера;
  вместо одиночной стрелки — заметная кнопка «Назад». Предметы обозначены
  словами, увеличены мелкие подписи и зоны нажатия, добавлены safe-area отступы.
- 52/52 теста и Vinext build успешны, lint без ошибок (3 прежних предупреждения).
- Браузерная проверка заблокирована `ERR_BLOCKED_BY_CLIENT` для внутреннего
  preview. Мобильная приёмка не выполнена, ветку пока не сливать вслепую.
- Код UI-005 не опубликован; production отдельно обновлён пакетом AUTH-002
  до `1cb8e5b57971249ee968d206a41b8025183552f8`.
- Отдельный детский вход создан и проверен; основной аккаунт сохранён. Сейчас
  оба аккаунта имеют одинаковый доступ, разделение ролей отложено.
- Точная привязка к учебникам (R8) отложена владельцем; известных данных
  достаточно для продолжения независимых улучшений, но не для заявления
  соответствия конкретному учебнику.

## Завершён AUTH-002 — второй детский вход

- Основной аккаунт владельца сохранён; второй детский аккаунт получает такие
  же права. Роли и ограничения пока намеренно не вводятся.
- Секреты не хранятся в GitHub. Закрытые переменные установлены только в VDS
  `.env`, режим файла — `600`; перед изменением окружения создана резервная копия.
- 53/53 теста подтверждают оба входа и запрещают смешивать логин одного
  аккаунта с паролем другого; обе production-сборки и TypeScript успешны.
- Контейнер `app` развёрнут из commit
  `1cb8e5b57971249ee968d206a41b8025183552f8` с `--no-deps`; health API
  успешен, детский вход на рабочем домене создаёт сессию и возвращает на
  главную.
- Перед публикацией создана копия
  `/home/zapolnaya28/vasilisa-backups/pre-auth002-20260908/` (1,2 МБ);
  окружение сохранено в
  `/home/zapolnaya28/vasilisa-backups/env-pre-child-20260908-175500`.
  Постоянный том, Caddy и Weight App не изменялись.

## Текущий пакет R7 — родительская аналитика

- Статус `REVIEW`; код и production обновлены до
  `ca9d53635b538236412d226fcbc610b804057e62`.
- В разделе «Для мамы» доступны периоды 7/14/30 дней, показатели первой
  попытки, самостоятельных исправлений, подсказок и среднего времени.
- Показаны состояния всех 24 Math/English-навыков, спокойные пояснения,
  слабые темы и ближайшая дата закрепления, если она назначена адаптивной
  системой.
- Безопасная JSON-выгрузка ограничена разрешёнными агрегатами и не содержит
  введённых или ожидаемых ответов, паролей, токенов и системных данных.
- API выполняет только чтение и агрегацию SQLite; тесты подтверждают, что
  просмотр и выгрузка не изменяют учебную историю.
- Пройдены 49/49 тестов, VDS production build, TypeScript,
  `git diff --check`; lint — 0 ошибок и 3 прежних предупреждения `<img>`.
- Перед публикацией создана копия данных
  `/home/zapolnaya28/vasilisa-backups/pre-r7-20260908/` (1,2 МБ). Обновлён
  только `app`; он healthy, health API и OCI revision проверены. Caddy,
  Weight App и постоянный том не изменялись.
- Визуальная приёмка взрослого экрана отложена владельцем, поэтому статус пока
  `REVIEW`. R8 заблокирован до получения точных изданий; R9 требует отдельного
  явного разрешения на сброс тестовых данных.

## Завершённый пакет R5 — разнообразные спокойные миссии

- Код и production обновлены до
  `0e8fe47f6093589d717289999f1c5993e5188a83`.
- Секретные миссии проходят 30 последовательных дней без точного повтора;
  домашние миссии расширены, а их заголовки и небольшие истории меняются по
  дате вместе с содержанием.
- Пулы содержат 32 сценария порядка, 32 добрых дела, 32 самостоятельных шага
  и 31 секретную миссию. Шесть игровых механик чередуются без повтора в
  соседние дни; дневные наборы не повторяются 30 дней.
- Пакет имеет статус `DONE`; сохранённые строковые выборы, история выполнения,
  награды и дневная нагрузка не менялись.
- 8 сентября пройдены 42/42 теста, обе production-сборки, TypeScript,
  `git diff --check`; lint — 0 ошибок и 3 прежних предупреждения `<img>`.
- Перед публикацией создан архив данных
  `/home/zapolnaya28/vasilisa-backups/pre-r5-final-20260908/data.tgz` (135K).
  Пересобран только `app` с `--no-deps`; контейнер healthy, локальный health
  API вернул `{"ok":true}`, OCI revision совпадает с commit.
- Caddy и Weight App не перезапускались. Фактически Weight App сейчас состоит
  из четырёх работающих контейнеров (`running(4)`); прежняя документация
  ошибочно ожидала пять и требует отдельной сверки владельцем соседнего проекта.

## Текущий пакет R6 — недельное приключение

- Статус `REVIEW`; код и production обновлены до
  `a3f3004e5d993f8fa0a97f7b414139852316fe7d`.
- Приложение выбирает один из пяти миров на календарную неделю и показывает
  отдельную главу каждого из семи дней: название, спокойную цель и следующий
  шаг.
- Компактная карта содержит семь шагов. Выполненные миссии из уже сохраняемого
  прогресса двигают маркер Василисы и открывают находки; после семи миссий
  появляется финальная сцена. Поэтому обновление страницы ничего не теряет.
- Пять солнечных фрагментов учитываются только в пределах текущей недели.
  Пропуск ничего не отнимает, а открытое семейное приключение не является
  обязательным заданием.
- Количество заданий, звёзды и деньги не менялись: максимум пять Math и пять
  English в день сохранён.
- Пройдены 46/46 тестов, обе production-сборки, TypeScript,
  `git diff --check`; lint — 0 ошибок и 3 прежних предупреждения `<img>`.
- Перед публикацией создан архив
  `/home/zapolnaya28/vasilisa-backups/pre-r6-20260908/data.tgz` (135K).
  Пересобран только `app`; сервис healthy, health API успешен, OCI revision
  совпадает. Caddy, Weight App и постоянный том не изменялись.
- До `DONE` нужна только короткая визуальная проверка карты на обычном слабом
  телефоне. Следующий пакет разработки — R7, родительская аналитика.

## Текущий пакет R4 — спокойный English

- Дневной English-набор ограничен пятью короткими заданиями: слово/написание,
  грамматика, сборка предложения карточками, короткое понимание и повторение.
- Простое задание «слово → ответ» встречается не чаще одного раза за день;
  сборка предложения по-прежнему выполняется нажатием на слова.
- Учебные emoji из заданий удалены: экран использует единый набор SVG.
- Код и тесты опубликованы в GitHub: `1ba1e0e`, `5976330`; локально прошли
  38/38 тестов, обе production-сборки, lint без ошибок (3 старых предупреждения).
- Production технически проверен 8 сентября в составе `3863af5`; живая детская
  приёмка отложена по решению владельца, поэтому статус остаётся `REVIEW`.

## Текущий пакет UI-004 — спокойные задания и печать

- На детских экранах Math и English убраны карточки «4 класс», «Школа России»,
  «Мягкая настройка уровня» и роли заданий. Вместо них есть короткое
  поддерживающее объяснение действия.
- Условия заданий стали заметнее, поле ответа и подсказка разнесены; на
  мобильной ширине Math переносит ответ под условие. English использует
  тематические inline-SVG вместо emoji, кликабельная сборка фраз сохранена.
- Обновлены такие же добрые вводные тексты остальных ежедневных разделов.
  Учебный лимит остался прежним: максимум пять Math и пять English в день.
- PDF больше не содержит «Мой день - моя история»; подпись и печать размещены
  раздельно, печать стала крупнее и читаемее: ООО «СловоМамы», г. Ковров,
  «ДЛЯ ДОКУМЕНТОВ», без лапки.
- Локально: 34/34 теста, lint без ошибок (3 прежних предупреждения `<img>`),
  Vinext build и VDS Next.js build успешны. На production проверены живые
  Math/English, контейнер `app` healthy, health API отвечает `{"ok":true}`.
- Код опубликован в GitHub `main` commit
  `0ac5c05644029400dab65a6630c7f430d4438171`; перед обновлением создана копия
  данных `pre-ui004-20260905/data.tgz`, обновлён только `app`.
- Статус `REVIEW`: на следующем уже подписанном дне нужно вручную открыть
  скачанный PDF и подтвердить визуально подпись, печать и отсутствие наложения.

## Текущий пакет R2 — подсказки и аналитика

- Локально готово переключаемое состояние подсказки: повторное нажатие скрывает
  текст, но не удаляет факт её использования из прогресса ребёнка.
- Явный запрос подсказки и первая неверная попытка сохраняют `hintUsed`; этот
  признак уже участвует в серверном адаптивном расчёте навыка.
- Экран ожидал шесть английских заданий при том, что генератор выдаёт пять;
  несоответствие исправлено, чтобы не возникала ложная ошибка загрузки.
- Подсказки оформлены компактнее и остаются отдельными для Math и English.
- Локальные проверки: 34/34 теста, lint без ошибок (3 прежних предупреждения
  `<img>`), Vinext build и VDS Next.js build с TypeScript успешны.
- Пакет опубликован на production; интерактивная приёмка открытия и закрытия
  подсказки на обычном устройстве отложена, поэтому статус остаётся `REVIEW`.

## Текущий пакет UI-003

- Чистый PDF-отчёт не содержит личный блок «Мой волшебный день»: настроение,
  победы, трудности и сообщение папе остаются только в приложении и постоянном
  серверном хранилище.
- Декоративная лапка заменена круглой синей печатью для документов с текстом
  ООО «СловоМамы», города Ковров и центром «ДЛЯ ДОКУМЕНТОВ».
- После закрытия дня экран показывает «Заверено мамой» и «Скачать отчёт» без
  прежнего рекламного подзаголовка.
- Финальный блок PDF содержит только «День принят!», поддерживающую фразу,
  московские дату и время, сохранённую подпись и печать.
- Пройдены 29/29 тестов, lint без ошибок, обе production-сборки и визуальная
  проверка двух страниц тестового A4 PDF.
- Код UI-003 опубликован в GitHub и на production из commit
  `ac6791f73be70bbd78915a0cd249b08c2ea332f6`.
- Перед обновлением создана копия
  `/home/zapolnaya28/vasilisa-backups/pre-ac6791f`; пересоздан только `app`.
  OCI revision совпадает с commit, сервис `healthy`, health API отвечает
  `{"ok":true}`, постоянные файлы сохранены. Caddy и пять контейнеров Weight
  App не перезапускались.
- Пакет UI-003 завершён. Следующий приоритет — мобильная приёмка R1, затем R2.

## Текущий пакет R1

- Сценарий чтения разделён на сохранение страниц, необязательный смысловой
  вопрос по текущему диапазону и однократное финальное поздравление.
- В дневном прогрессе появилась связанная запись `readingSession`: дата, книга,
  диапазон, время, вопросы, ответы и состояние завершения.
- Третья звезда вычисляется сервером только по правильным ответам текущего дня;
  унаследованные ответы прошлых дней на награду не влияют.
- Карта чтения содержит 17 важных смысловых контрольных точек по трём книгам.
  При любом диапазоне появляется не больше одного вопроса; между контрольными
  точками вопроса нет.
- После последней страницы книга предлагает персональную книжную заметку
  Василисы. Текст сохраняется без оценки, а за завершённую книгу и заметку один
  раз начисляется отдельный папин бонус `10 ⭐` вне дневного лимита.
- Детская обратная связь переписана в поддерживающем тоне, красное состояние
  повторной попытки заменено на спокойное нейтральное оформление.
- Локально пройдены 26 автоматических тестов, Vinext production build, lint без
  ошибок и VDS Next.js build с TypeScript-проверкой.
- Код R1 опубликован в GitHub `main` commit
  `e162146f5934d8acf93ad57f4c70d9fc2501e4fc`.
- Production обновлён до server HEAD
  `a5385c3910d9a354321a65a816238c68bcb20c87`.
- Контейнер приложения и пять контейнеров Weight App имеют статус `healthy`,
  `/api/health` возвращает `{"ok":true}`, постоянные данные сохранены.
- Авторизованная приёмка подтвердила вход, главную, чтение, Math и English.
  Открытые `#reading`, `#math` и `#english` сохраняются после обновления.
  Пакет остаётся `REVIEW` только до живой мобильной проверки незавершённого
  сценария чтения.

## Рабочая версия

- Основной домен: <https://nectarra.ru/>
- Канонический публичный репозиторий:
  <https://github.com/medok33/vasilisa-adventures>.
- Исходники Sites и GitHub объединены без переписывания истории. GitHub
  назначен единственным источником будущих обновлений VDS.
- VDS обновлён из GitHub 1 сентября 2026 года: пересобран и пересоздан только
  сервис `app`, Caddy и Weight App не перезапускались.
- Текущий production commit:
  `ca9d53635b538236412d226fcbc610b804057e62`.
- Compose-проект: `vasilisa-adventures`.
- Приложение: `vasilisa-adventures-app-1`, `127.0.0.1:3100`.
- Прокси: `vasilisa-adventures-caddy-1`, порты `80/443`.
- Постоянный том: `vasilisa-adventures-data`.

## Реализовано

- семь ежедневных миссий;
- уникальный набор математики, английского, порядка, добрых дел и самостоятельности на каждую московскую дату;
- сохранение числа попыток по математике и английскому без штрафов;
- подробная история каждой проверки по каждому учебному вопросу: первый и
  последующие ответы, верность, номер попытки и время проверки;
- серверная авторизация всего сайта и API по логину и паролю;
- HttpOnly-сессия с HMAC-подписью и сроком семь дней;
- закрытые API прогресса, учебных заданий и контактов папы; публичным остаётся
  только `/api/health`;
- SQLite `/data/learning.sqlite` с заданиями, попытками и прогрессом навыков;
- взрослая аналитика за 7/14/30 дней и безопасная JSON-выгрузка агрегатов;
- секретная миссия дня и собственный вариант ответа;
- недельная цель из пяти солнечных фрагментов;
- пять сменяющихся недельных миров, семь ежедневных глав, сюжетная карта,
  находки за готовые миссии и финальная сцена дня;
- последовательная библиотека из трёх проверенных изданий с ISBN, диапазонами страниц и счётчиком завершённых книг;
- накопительный учёт прочитанных диапазонов с автоматическим переносом прежних полей страниц;
- вопросы только по уже прочитанной части книги;
- 17 автоматических вопросов по трём книгам с привязкой к диапазонам страниц
  и вариантами ответа;
- третья звезда чтения только за верный вариант ответа; её автоматически
  пересчитывает сервер без ручной проверки взрослого;
- изменение оформления иконок выполненных миссий;
- адаптивная главная страница и анимированное настроение Василисы;
- стабильная навигация между разделами;
- дневник «Мой день» с историей по датам;
- отдельные поля «Получилось», «Не получилось» и «Просьбы папе»;
- звонок папе и переходы в VK/MAX;
- распределение награды между лимитом и копилкой с шагом 10 рублей;
- раздел «Для мамы» для проверки и закрытия дня;
- подпись мамы пальцем;
- PDF-отчёт с маминой подписью и круглой синей печатью для документов;
- сохранение прогресса в отдельном постоянном хранилище.

## Последний опубликованный пакет

- Раздел взрослой проверки снова называется «Для мамы»: мама подтверждает
  бытовые миссии, закрывает день и ставит подпись.
- Папины контакты, сообщения в дневнике и персональный бонус за книжную заметку
  остались отдельными папиными функциями.
- PDF полностью переработан: современная мультяшная композиция, карточки
  результата, отдельный блок маминой проверки и синяя круглая печать с лапкой.
- Совместимый внутренний ключ `motherSignature` сохранён без миграции, поэтому
  прежние подписи и закрытые дни не потеряны.
- Локально пройдены 29/29 тестов, Vinext production build, lint без ошибок
  (три прежних предупреждения `<img>`) и VDS Next.js build с TypeScript.
- Код опубликован в GitHub и на VDS commit
  `a5385c3910d9a354321a65a816238c68bcb20c87`.
- Перед публикацией создана копия `/data` в
  `/home/zapolnaya28/vasilisa-backups/pre-a5385c3`.
- Пересобран и пересоздан только сервис `app` с `--no-deps`; Caddy, Weight App,
  сети и постоянный том не перезапускались и не пересоздавались.
- OCI revision-label совпадает с production commit, контейнер имеет статус
  `healthy`, health API возвращает `{"ok":true}`, логи без ошибок запуска.
- Авторизованная приёмка подтвердила главную и экран «Для мамы» с маминой
  подписью и готовым PDF. Задания, подпись и прогресс ребёнка не изменялись.

## Не завершено

- Автоматическая доставка сообщений папе во VK/MAX не подключена. Личная
  страница VK не даёт приложению права отправлять уведомления. Нужен отдельный
  официальный бот/сообщество и его безопасные серверные настройки.
- Страницы и формулировки 17 смысловых контрольных точек нужно окончательно
  сверить с фактическими изданиями книг; намеренно не планируется покадровый
  опрос по каждой главе.
- Точное соответствие English школьному учебнику требует названия и издания
  учебника; текущая стартовая карта соответствует базовому уровню 3 класса.

## Инфраструктурные ограничения

- На VDS также работает Weight App. Его нельзя перезапускать или изменять.
- Обновление сайта выполняется пересборкой только сервиса `app`; Weight App не затрагивается.
- Том прогресса нельзя удалять или пересоздавать.
- Docker label production показывает рабочий каталог
  `/opt/vasilisa-adventures/source/vds`; OCI revision-label совпадает с
  текущим production commit. Новые образы по-прежнему собираются с
  `APP_REVISION=$(git rev-parse HEAD)`.

## Правило актуализации

После каждого законченного пакета правок обновить этот документ: указать
результат сборки, состояние VDS и оставшиеся задачи.
